<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Chemical Management</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="login-body">
    <div class="login-container">
        <h2>Login</h2>
        <p>Surplus Chemical Management System</p>
        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="username">Username</label>
                <input type="text" id="username" name="username" required
                placeholder="e.g., ADMIN">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required
                placeholder="e.g., ADMIN">
            </div>
            <button type="submit">Login</button>
        </form>
        <div class="credentials">
            <h4>Demo Logins:</h4>
            <p>User: ADMIN / Pass: ADMIN</p>
            <p>User: INVENTORYMANAGER / Pass: ADMIN_INVENTORYMANAGER</p>
            <p>User: PRODUCTIONMANAGER / Pass: ADMIN_PRODUCTIONMANAGER</p>
            <p>User: RETAILMANAGER / Pass: ADMIN_RETAILMANAGER</p>
        </div>
    </div>
</body>
</html>